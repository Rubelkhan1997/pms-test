# CRUD Module Generator

> Read ALL reference files before writing anything.
> Replace every [PLACEHOLDER] before generating.
> Generate files in the exact order listed. Wait for confirmation after each file.

---

## Module Info
- Module Name:      [MODULE_NAME]      ← Namespace/Folder  → e.g. FrontDesk
- Feature Name:     [FEATURE_NAME]     ← Sub-feature name  → e.g. Reservation
- Model Name:       [MODEL_NAME]       ← Model class name  → e.g. Reservation
- Table Name:       [TABLE_NAME]       ← e.g. reservations
- Route Slug:       [ROUTE_SLUG]       ← e.g. reservations
- Route Prefix:     [ROUTE_PREFIX]     <- e.g. front-desk.reservations
- Is New Module:    [YES|NO]           ← YES = new module, NO = add to existing
- Fields:           [FIELD_LIST]       ← e.g. title:string, room_id:foreignId, note:text|nullable
- Relationships:    [RELATIONSHIPS]    ← e.g. belongsTo Room
- Status Enum:      [STATUS_VALUES]    ← e.g. pending, in_progress, done
- Has Events/Jobs:  [YES|NO]

---

## Module Rules
- Is New Module = YES -> Create Module folder, ServiceProvider, config
- Is New Module = NO  -> Only create Feature files, do not touch module files
- Route Prefix:     [ROUTE_PREFIX]     <- e.g. front-desk.reservations
- Frontend path always -> Pages/[FEATURE_NAME_PLURAL]/
- Pinia store path -> Stores/[MODULE_NAME]/[model_name]Store.ts
- Types path -> Types/[MODULE_NAME]/[model_name].ts

---

## File Generation Order

> Is New Module = YES → সব files তৈরি করো
> Is New Module = NO  → Module-level files (ServiceProvider, config) skip করো, শুধু Feature files তৈরি করো
```
Backend:
1.  Migration
2.  Enum (app/Enums/[MODEL_NAME]Status.php)
3.  Model
4.  Data/DTO ([MODEL_NAME]Data.php)
5.  Store[MODEL_NAME]Request.php
6.  Update[MODEL_NAME]Request.php
7.  Resource ([MODEL_NAME]Resource.php)
8.  Policy
9.  Observer
10. Action (Create[MODEL_NAME]Action.php)
11. Service
12. API Controller (Api/V1/)
13. Web Controller (Web/) — Inertia responses only
14. Routes (api.php + web.php)
[If Has Events/Jobs = YES]
15. Event ([MODEL_NAME]Created.php)
16. Listener
17. Job

Frontend:
18. Types      (resources/js/Types/[MODULE_NAME]/[model_name].ts)
19. Mappers    (resources/js/Utils/Mappers/[model_name].ts)
20. Pinia Store (resources/js/Stores/[MODULE_NAME]/[model_name]Store.ts)
21. Composable  (resources/js/Composables/[MODULE_NAME]/use[MODEL_NAME].ts)
22. Index.vue   (resources/js/Pages/[FEATURE_NAME_PLURAL]/Index.vue)
23. Create.vue  (resources/js/Pages/[FEATURE_NAME_PLURAL]/Create.vue)
24. Edit.vue    (resources/js/Pages/[FEATURE_NAME_PLURAL]/Edit.vue)
25. Show.vue    (resources/js/Pages/[FEATURE_NAME_PLURAL]/Show.vue)

Tests:
26. Feature Test (tests/Feature/[MODULE_NAME]/[MODEL_NAME]Test.php)
```

---

## Backend Specs

### Migration
- id, [FIELD_LIST], timestamps, softDeletes if needed
- foreignId → constrained()

### Enum
```php
<?php declare(strict_types=1);
namespace App\\Enums;

enum [MODEL_NAME]Status: string {
    case Pending = 'pending';
    // match [STATUS_VALUES]
    public function label(): string { ... }
}
```

### Model
- `declare(strict_types=1)`
- `$fillable`, `$casts` (enum cast for status), relationships typed
- SoftDeletes if applicable

### Data/DTO
- Typed properties for all [FIELD_LIST]
- Match pattern in `app/Modules/FrontDesk/Data/ReservationData.php`

### Requests
- `authorize()` → `$this->user()->can(...)`
- `rules()` fully typed
- UpdateRequest uses `sometimes` where appropriate

### Resource
Always output:
```php
'id'           => $this->id,
'status'       => $this->status->value,
'status_label' => $this->status->label(),
'created_at'   => $this->created_at?->toISOString(),
'updated_at'   => $this->updated_at?->toISOString(),
// all [FIELD_LIST] fields
```

### Policy
- viewAny, view, create, update, delete, restore, forceDelete

### Observer
- created, updated, deleted hooks
- Dispatch events if Has Events = YES

### Action
- Single public `handle()` method
- Receives Data object, returns Model

### Service
- `paginate(array $filters)` — filterable by [FIELD_LIST] searchable fields
- `find(int $id): [MODEL_NAME]` — throws ModelNotFoundException
- `create([MODEL_NAME]Data $data): [MODEL_NAME]` — uses Action
- `update(int $id, [MODEL_NAME]Data $data): [MODEL_NAME]`
- `delete(int $id): void`
- DB::transaction() when writing multiple tables

### API Controller
- Thin — only: authorize, validate, call service, return response
- Response format: `{ status: 1|0, data: ..., message: string }`
- Pagination response:
```json
{
  "status": 1,
  "data": {
    "items": [],
    "pagination": { "current_page": 1, "per_page": 15, "total": 0, "last_page": 1 }
  },
   "message": "..."
}
```
- HTTP codes: 200 / 201 / 204 / 422 / 404

### Web Controller
- Returns Inertia responses only
- `index()`  → `Inertia::render('[FEATURE_NAME_PLURAL]/Index')`
- `create()` → `Inertia::render('[FEATURE_NAME_PLURAL]/Create')`
- `edit()`   → `Inertia::render('[FEATURE_NAME_PLURAL]/Edit',   ['item' => ...])`
- `show()`   → `Inertia::render('[FEATURE_NAME_PLURAL]/Show',   ['item' => ...])`

### Routes
```php
// api.php
Route::middleware('auth:sanctum')
    ->prefix('v1/[ROUTE_SLUG]')
    ->name('[ROUTE_PREFIX].')
    ->group(fn() => [
        Route::get('/',      [[MODEL_NAME]ApiController::class, 'index'])  ->name('index'),
        Route::post('/',     [[MODEL_NAME]ApiController::class, 'store'])  ->name('store'),
        Route::get('/{id}',  [[MODEL_NAME]ApiController::class, 'show'])   ->name('show'),
        Route::put('/{id}',  [[MODEL_NAME]ApiController::class, 'update']) ->name('update'),
        Route::delete('/{id}',[[MODEL_NAME]ApiController::class,'destroy'])->name('destroy'),
    ]);

// web.php
Route::middleware('auth')
    ->prefix('[ROUTE_SLUG]')
    ->name('[ROUTE_PREFIX].')
    ->group(fn() => [
        Route::get('/',           [[MODEL_NAME]WebController::class, 'index']) ->name('index'),
        Route::get('/create',     [[MODEL_NAME]WebController::class, 'create'])->name('create'),
        Route::get('/{id}',       [[MODEL_NAME]WebController::class, 'show'])  ->name('show'),
        Route::get('/{id}/edit',  [[MODEL_NAME]WebController::class, 'edit'])  ->name('edit'),
    ]);
```

---

## Frontend Specs

### Types
- Import `ApiResponse`, `PaginatedResponse` from `@/Types/api.ts`
- Define: `[MODEL_NAME]`, `[MODEL_NAME]Status`, `Create[MODEL_NAME]Dto`, `Update[MODEL_NAME]Dto`, `[MODEL_NAME]Filters`
- camelCase in all TS types

### Mappers
- `mapTo[MODEL_NAME]()` → API snake_case → UI camelCase
- `mapTo[MODEL_NAME]Payload()` → UI camelCase → API snake_case

### Pinia Store
- State shape:
```ts
items:        [MODEL_NAME][]
selectedItem: [MODEL_NAME] | null
loading:      boolean        // save/delete
loadingList:  boolean        // fetch
error:        string | null
pagination: {
    data: [MODEL_NAME][]
    meta: { current_page, last_page, per_page, total }
}
```
- Use mapper functions for API ↔ UI conversion
- Guard with `Array.isArray` before map/filter

### Composable
- `options?: { autoFetch?, initialFilters?, pollingInterval? }`
- Use `useLoading`, `useMessage`, `usePolling`
- `inject('$toast')`, `inject('$confirm')`
- Delete confirms via `$confirm.show({ variant: 'danger', ... })`
- Never expose store directly

### Pages
- **Index.vue**  : search, status filter, `<Table>`, pagination, edit/delete per row
- **Create.vue** : typed `reactive()` form, `try/catch`, `error.response?.status === 422`
- **Edit.vue**   : receives `item` as Inertia prop, pre-filled form, same pattern as Create
- **Show.vue**   : read-only detail view, back + edit buttons
- All use `<AppLayout>`, `<AppButton>`, `<AppInput>`

### Page Pattern (Create/Edit)
```ts
// ✅ Standard pattern — Login/Register/Reservation এর মতো
const form = reactive({ ... })
const errors = reactive<Record<string, string>>({ ... })

async function submit(): Promise<void> {
    Object.keys(errors).forEach(key => errors[key] = '')
    if (!validateForm()) return
    try {
        await create(form)   // বা update()
        router.visit('/[ROUTE_SLUG]')
    } catch (error: any) {
        if (error.response?.status === 422) {
            const apiErr = error.response.data
            // map errors to reactive errors object
        }
    }
}
```

---

## Conventions
- Pages are at resources/js/Pages/[FEATURE_NAME_PLURAL]/ (no module folder)
- PHP -> snake_case for fields/arrays
- TS/JS -> camelCase in UI/types
- Helpers -> pure functions only (no side effects)
- Side effects (localStorage, cookies, DOM) -> Utils/ only

---
## Auth Flow Note
- Web (Inertia) → session auth
- API → Sanctum token
- Login/Register → `with_token` boolean controls token issuance

---

## Tests
```
✓ unauthenticated → 401 on all endpoints
✓ index returns paginated list
✓ index filters by status
✓ show returns single item
✓ show → 404 for missing id
✓ store creates → 201 with correct structure
✓ store → 422 on validation failure
✓ update → 200
✓ update → 404 for missing id
✓ destroy → 204
✓ destroy → 404 for missing id
✓ unauthorized user → 403
```








